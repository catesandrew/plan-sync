RALPH PROGRESS LOG — omc-sync implementation
Source plan: .omc/plans/shadow-ref-git-sync-for-omc-artifacts.md (v3)
PRD: .omc/prd.json (legacy project-level path; no session ID was available to derive a session-scoped path)
STATUS: All 9 original stories pass, plus US-010 (security hardening). 73/73 tests green after round-2 fixes, but the round-3 completion review REJECTED again: the round-2 symlink-destination guard (duplicated in restore.ts and sibling/push.ts) was itself bypassable two ways, plus a new regression (process.cwd()-based root resolution + missing-vs-empty manifest confusion) could silently commit an empty tree and cause restore elsewhere to delete real local files. Reviewer explicitly recommended a STRUCTURAL fix (shared guard module + git-toplevel-anchored repoRoot + manifest-missing distinction) rather than a fourth narrow patch — implementing that now.

Decisions made (not in the plan, needed for implementation):
- Stack: Node.js + TypeScript (CJS), vitest, plain arg parsing.
- Both Part A (sibling-repo) and Part B (single-machine shadow-ref) in scope.
- Manifest file: `.omc/.sync-manifest`, one path per line, shared by both tracks, opt-in only.
- Track routing via `--track sibling|shadow` in src/commands/*.ts.
- US-005: ".gitattributes" amended to "info/attributes + core.attributesFile" (bare repos have no working tree) — verified legitimate.

Completion review history (codex-bridge failed twice at the start of Step 7 — MCP timeout, then transport dropped — all rounds below ran via the architect agent per ralph's "never block on unavailable tools" rule):
- Round 1: REJECTED. HIGH-1 (destructive scan-skip -> restore deletes local files), HIGH-2 (push crash on ordinary deleted file), MEDIUM-1 (manifest path traversal), MEDIUM-2 (read-side symlink dereference). All 4 fixed.
- Round 2: REJECTED (re-verifying round 1). Confirmed all 4 round-1 fixes genuine, but found N1 (push's "nothing to push" guard gated on survivor-count instead of tree-identity, so deleting EVERY manifest file silently discarded the deletion) and N2 (write-side symlink guard missing in restore.ts/sibling-push.ts, distinct from the round-1 read-side guard). Both fixed.
- Round 3: REJECTED (re-verifying N1/N2). N1 confirmed correct. N2's fix found bypassable two ways (existsSync misses dangling symlinks; guard only checked immediate parent, not walking up to the nearest existing ancestor) with reproduced probes. Also found N1's fix opened a new regression: process.cwd()-based repoRoot vs git-repo-based projectId can disagree (e.g. running from a subdirectory), and readManifest conflates "missing" with "empty" — together these allow a push to silently commit an empty tree, which a later restore elsewhere then reads as "genuinely deleted," destroying real local files with no attacker required. Reviewer's meta-observation: three rounds each found a new narrow instance of the same two bug classes from copy-pasted, ad-hoc guards; recommended a single shared, correctly-written guard plus anchoring repoRoot to `git rev-parse --show-toplevel` plus a manifest missing-vs-empty distinction, instead of a fourth narrow patch.

Iteration log:
- Iter 1-11: US-001..US-009 implemented, verified, 61 tests green.
- Iter 12: codex-bridge failed twice; fell back to architect. Round 1 REJECTED (4 findings, see above).
- Iter 13: fixed round-1 findings + added US-010. 69 tests green.
- Iter 14: round 2 REJECTED (N1 + N2, see above).
- Iter 15: fixed N1 + N2 in parallel (disjoint files). 73 tests green.
- Iter 16: round 3 REJECTED (structural, see above). Delegating one comprehensive structural fix per the reviewer's explicit recommendation (shared src/safe-write.ts guard; repoRoot anchored via git rev-parse --show-toplevel across all track/command call sites; manifest missing-vs-empty distinction gating shadow push).
- Iter 17: structural fix landed — new src/safe-write.ts (lstat-based dangling-symlink detection + walk-up-to-nearest-existing-ancestor realpath containment, shared by restore.ts and sibling/push.ts) and src/repo-root.ts (resolveRepoRoot via `git rev-parse --show-toplevel`, used everywhere repoRoot was previously raw process.cwd() except sibling/init.ts's user-facing --clone-path resolution, deliberately left as literal cwd-relative). manifest.ts gained manifestExists(); shadow push now refuses (throws) rather than committing an empty tree when the manifest file is missing entirely and a real previous tip exists. 79/79 tests green.
- Iter 18: round 4 REJECTED. The round-3 guard's logic was confirmed correct, but the reviewer enumerated every filesystem mutation across both tracks and found it was only wired into 2 of 6 actual destination-mutation call sites: restore.ts's DELETE path (only its write path was guarded), sibling/pull.ts (no guard at all, on either copy-in or delete-propagation — this track had zero destination-safety coverage), and sibling/push.ts's deletion branch (returns via `continue` before ever reaching the guard). Also found the guard could throw (uncaught ENOENT/ENOTDIR) instead of failing closed, and init hardcodes `.git/info/exclude` in a way that breaks inside a linked git worktree. Reviewer's root-cause diagnosis: a shared predicate you must remember to call at each site has the same failure mode as copy-pasted guards. Recommended converting safe-write.ts into safe OPERATION functions (safeWriteFile/safeCopyFile/safeRemove) that are the only way to mutate a destination, applied to all 6 sites, made fail-closed, plus a structural regression test that scans for any future unguarded destination mutation so a missed site fails CI instead of a fifth review round.
- Iter 19: choke-point refactor landed — safe-write.ts rewritten as safeWriteFile/safeCopyFile/safeRemove (fail-closed, try/catch around all realpath/lstat resolution); restore.ts's delete path, sibling/pull.ts (both copy-in and delete, previously fully unguarded, plus a missing symlink-source skip), and sibling/push.ts's deletion branch all now route through these; both init.ts files resolve `.git/info/exclude` via `git rev-parse --git-path info/exclude` for worktree support; added a structural test (test/no-unguarded-writes.test.ts) scanning src/tracks/**/*.ts for any raw fs.rmSync/writeFileSync/copyFileSync outside safe-write.ts's own internals. 101/101 tests green.
- Iter 20: round 5 completion review APPROVED. Reviewer independently re-derived the full destination-mutation enumeration (not trusting prior classification) and found zero remaining unguarded manifest-derived mutations; re-probed every escape shape from rounds 1-4 and confirmed all closed. Noted 3 non-blocking follow-ups for future hardening (not required for this sign-off): F1 safeRemove throws EISDIR on a directory manifest entry (pre-existing input class, not a regression, fails loud inside the containment root -- explicit note: do NOT fix by adding recursive:true, that would grant restore recursive-delete authority via git log prefix-matching; correct fix is rejecting directories in addToManifest + an isDirectory guard); F2 the structural no-unguarded-writes test's pattern list is narrower than the full mutating-fs-API surface (covers exactly what the acceptance criterion specified, extending to aliased imports/rmdirSync/cpSync/etc. and to src/commands+manifest.ts is a worthwhile follow-up); F3 hardlink write-through is an inherent limitation of realpath-based containment (requires attacker to already have write access inside .omc/, no AC covers it, temp-file+rename would close it if ever wanted).
- Iter 21: mandatory deslop pass (Step 7.5) run directly -- no debug leftovers, no TODOs, no dead code found; file sizes proportionate to the git-plumbing logic implemented. Treated as a clean no-op given the code just cleared 5 rounds of adversarial review; did not touch any hardened logic. Post-deslop regression (Step 7.6): npm run build clean, npm test 101/101 passing. Running /oh-my-claudecode:cancel (Step 8).

test edit Sun Sep  6 13:56:18 PDT 2026
